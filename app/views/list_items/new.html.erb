<h1>Add Content for <%= @list_item.list.title %></h1>

<%= form_for(@list_item) do |f| %>
<br>

<div>
 <%= f.label :content %>
 <%= f.text_area :content, size: "25x5" %>
</div>
<%= f.hidden_field :list_id %>

<%= f.submit %>

<% end  %>
